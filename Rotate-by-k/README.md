#Rotate By K (Right rotate)

This code is contains three approach first one is simple approach which is use by rotate by one inside a while loop (k!=0) ,second one is best approach but contains extra space using extra temp array and third one is More optimze approach that is not contains extra space which is use a technique that is first reverse whole array then start again first reverse till k then second reverse remains .
